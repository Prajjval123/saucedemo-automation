[pytest]
# tell pytest to look for test files inside the tests folder
testpaths = tests

# -v shows each test name and pass/fail clearly in the terminal
# --tb=short shows a short error message if a test fails
addopts = -v --tb=short
